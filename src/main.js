const { mdLinks } = require('./mdLinks.js');

const link = './src/doc/';
// mdLinks(link, true).then((msg) => {
//   console.log(msg);
// });
// C:/Users/Eunice Fiorella/testgit/LIM014-mdlinks/src/doc/
mdLinks(link, true).then((msg) => {
  console.log(msg);
});
